import inf from 'inf-lib';
import { Keypair, PCommand, blankStateLeafHash } from "maci-domainobjs";

// const value = blankStateLeafHash.toString();

// console.log(value);
// console.log(inf.bn_to_bytes_be({ value }).value.join(', '));
